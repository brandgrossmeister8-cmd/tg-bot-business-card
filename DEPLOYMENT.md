# Инструкция по деплою бота на сервер

Этот проект настроен для автоматического деплоя через GitHub Actions при каждом push в ветку `main`.

## 📋 Предварительные требования

На сервере должны быть установлены:
- Node.js (версия 16 или выше)
- npm
- PM2 (глобально): `npm install -g pm2`
- Git

## 🔧 Настройка сервера

### 1. Подключение к серверу

```bash
ssh your_user@your_server_ip
```

### 2. Установка Node.js (если не установлен)

```bash
# Для Ubuntu/Debian
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# Проверка установки
node -v
npm -v
```

### 3. Установка PM2

```bash
sudo npm install -g pm2

# Настройка автозапуска PM2 при перезагрузке сервера
pm2 startup
# Выполните команду, которую выведет PM2
```

### 4. Клонирование репозитория

```bash
# Создайте директорию для проекта (например, /var/www/marketing-bot)
sudo mkdir -p /var/www/marketing-bot
sudo chown $USER:$USER /var/www/marketing-bot

# Клонируйте репозиторий
cd /var/www
git clone https://github.com/brandgrossmeister8-cmd/tg-bot-business-card.git marketing-bot
cd marketing-bot
```

### 5. Создание .env файла

```bash
# Создайте .env файл с вашими переменными окружения
nano .env
```

Содержимое .env:
```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
SUPABASE_URL=your_supabase_url_here
SUPABASE_KEY=your_supabase_anon_key_here
```

### 6. Установка зависимостей и первый запуск

```bash
npm install
pm2 start ecosystem.config.js --env production
pm2 save
```

### 7. Проверка работы

```bash
# Посмотреть статус
pm2 status

# Посмотреть логи
pm2 logs marketing-bot

# Посмотреть последние 50 строк логов
pm2 logs marketing-bot --lines 50
```

## 🔐 Настройка GitHub Secrets

Для автоматического деплоя необходимо добавить секреты в GitHub репозиторий.

### 1. Создание SSH ключа на вашем локальном компьютере

```bash
# Генерируем новый SSH ключ (если еще не создан)
ssh-keygen -t ed25519 -C "github-actions-deploy" -f ~/.ssh/github_actions_deploy

# Копируем публичный ключ на сервер
ssh-copy-id -i ~/.ssh/github_actions_deploy.pub your_user@your_server_ip

# Тестируем подключение
ssh -i ~/.ssh/github_actions_deploy your_user@your_server_ip
```

### 2. Добавление секретов в GitHub

Перейдите в ваш репозиторий на GitHub:
**Settings → Secrets and variables → Actions → New repository secret**

Добавьте следующие секреты:

| Имя секрета | Значение | Пример |
|------------|----------|---------|
| `SSH_PRIVATE_KEY` | Содержимое файла `~/.ssh/github_actions_deploy` (приватный ключ) | `-----BEGIN OPENSSH PRIVATE KEY-----\n...` |
| `SERVER_HOST` | IP адрес или домен вашего сервера | `123.456.789.0` или `example.com` |
| `SERVER_USER` | Имя пользователя на сервере | `root` или `ubuntu` |
| `SERVER_PATH` | Путь к директории проекта на сервере | `/var/www/marketing-bot` |

### Как получить приватный ключ для GitHub:

```bash
cat ~/.ssh/github_actions_deploy
```

Скопируйте **полностью весь вывод**, включая строки:
```
-----BEGIN OPENSSH PRIVATE KEY-----
...
-----END OPENSSH PRIVATE KEY-----
```

## 🚀 Автоматический деплой

После настройки всех секретов, деплой будет происходить автоматически:

1. Вы делаете `git push` в ветку `main`
2. GitHub Actions автоматически:
   - Подключается к серверу по SSH
   - Получает последние изменения из репозитория
   - Устанавливает зависимости
   - Перезапускает бота через PM2

### Отслеживание деплоя

Вы можете отслеживать процесс деплоя в GitHub:
**Actions** → выберите последний workflow run

## 🔄 Ручной деплой (альтернатива)

Если нужно задеплоить вручную без GitHub Actions:

```bash
# На сервере
cd /var/www/marketing-bot
git pull origin main
npm install
pm2 restart marketing-bot
```

## 📊 Мониторинг бота на сервере

```bash
# Статус процесса
pm2 status marketing-bot

# Живые логи
pm2 logs marketing-bot

# Перезапуск
pm2 restart marketing-bot

# Остановка
pm2 stop marketing-bot

# Удаление из PM2
pm2 delete marketing-bot

# Информация о процессе
pm2 info marketing-bot

# Мониторинг ресурсов
pm2 monit
```

## 🛠 Устранение проблем

### Бот не запускается

1. Проверьте логи:
```bash
pm2 logs marketing-bot --lines 100
```

2. Проверьте .env файл:
```bash
cat /var/www/marketing-bot/.env
```

3. Проверьте права доступа:
```bash
ls -la /var/www/marketing-bot
```

### GitHub Actions не может подключиться к серверу

1. Проверьте, что SSH ключ добавлен на сервер:
```bash
cat ~/.ssh/authorized_keys
```

2. Проверьте, что приватный ключ корректно добавлен в GitHub Secrets

3. Проверьте логи GitHub Actions для деталей ошибки

### 409 Conflict ошибка

Если видите ошибку "409 Conflict: terminated by other getUpdates request":

```bash
# Остановите все процессы бота
pm2 delete all
pkill -9 -f "node.*bot.js"

# Запустите заново
cd /var/www/marketing-bot
pm2 start ecosystem.config.js --env production
pm2 save
```

## 📝 Обновление переменных окружения

Если нужно обновить токены или другие переменные:

```bash
# На сервере
nano /var/www/marketing-bot/.env
# Внесите изменения и сохраните

# Перезапустите бота
pm2 restart marketing-bot
```

## 🔒 Безопасность

- ✅ Никогда не коммитьте .env файл в Git
- ✅ Храните приватные SSH ключи в безопасности
- ✅ Используйте GitHub Secrets для конфиденциальных данных
- ✅ Регулярно обновляйте Node.js и зависимости
- ✅ Настройте firewall на сервере

## 📞 Контакты

Если возникли вопросы по деплою, обратитесь к документации:
- [PM2 Documentation](https://pm2.keymetrics.io/docs/usage/quick-start/)
- [GitHub Actions Documentation](https://docs.github.com/en/actions)
